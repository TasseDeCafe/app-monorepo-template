import { CustomerioUser } from '../types'

export const mockUpdateCustomer = async (
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  userId: string,
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  data: Partial<CustomerioUser>
): Promise<boolean> => {
  return true
}
