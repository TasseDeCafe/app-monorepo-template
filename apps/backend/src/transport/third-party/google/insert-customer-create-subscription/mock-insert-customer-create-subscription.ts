import Stripe from 'stripe'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const mockInsertCustomerCreateSubscription = async (subscription: Stripe.Subscription): Promise<void> => {
  return
}
