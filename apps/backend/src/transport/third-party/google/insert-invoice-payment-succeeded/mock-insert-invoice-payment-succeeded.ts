import Stripe from 'stripe'

export const mockInsertInvoicePaymentSucceeded = async (
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  invoice: Stripe.Invoice,
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  paymentNumber: number
): Promise<void> => {
  return
}
