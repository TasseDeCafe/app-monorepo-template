'use server'

import React from 'react'
import { PricingSection } from '@/app/[lang]/(components)/(pricing)/pricing-section'

export const TranslatedPricingSection = async () => {
  return <PricingSection />
}
